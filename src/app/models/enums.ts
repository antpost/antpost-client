export enum PostType {
    Message = 1,
    Link = 2,
    Sale = 3,
    Image = 4,
    Video = 5
}

export enum SchedulePostStatus {
    Running = 1,
    Pause = 2,
    Stop = 3
}

export enum ScheduleType {
    Post = 1,
    Join = 2,
    Comment = 3
}
