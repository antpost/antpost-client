export enum PostType {
    Message = 1,
    Link = 2,
    Sale = 3,
    Image = 4,
    Video = 5
}

export enum SchedulePostStatus {
    Opened = 0,
    Running = 1,
    Paused = 2,
    Stopped = 3
}

export enum ScheduleType {
    Post = 1,
    Join = 2,
    Comment = 3
}
